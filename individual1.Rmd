---
title: "individual1"
author: "cyber-atlas"
date: "April 11, 2019"
output: html_document
---

```{r}
library('ggplot2')

acc<- read.csv('accident.csv', stringsAsFactors = FALSE)


person <- read.csv('person.csv', stringsAsFactors = FALSE)

names(acc)
names(person)

```

Assuming the first day of the week is Sunday

```{r}
acc$DAY_WEEK
tab <- table(acc$DAY_WEEK)

tab

max(tab)

min(tab)

max.col(tab)
names(tab)[which.min(apply(tab,MARGIN=2,min))]
  which.max(apply(df,MARGIN=2,max))
head(tab)

```

```{r}

head(acc)

lab <- c("Sunday", 'Monday', 'Tuesday', 'Wednesday', 'Thursday', 'Friday', 'Saturday')
ggplot(data=acc, aes(x=DAY_WEEK, color= DAY_WEEK, fill=stat(count)))+
  geom_bar()+
  #geom_bar(aes(y=stat(count)))+
  geom_text(stat='count', aes(label=..count..), vjust = -.5)+
  scale_y_continuous(limits=c(0,6500))



```
